export const meta = {
  number: '01',
  slug: 'why-bayesian-optimization',
  title: 'Why Bayesian optimization? Sample efficiency, not scale.',
  summary:
    'Training a neural network, running a lab assay, and calibrating a qubit share one property: each evaluation is expensive and you cannot afford many. Bayesian optimization formalizes the surrogate-plus-acquisition loop that gets the most information per query.',
  reading: '9 min',
};

# Why Bayesian optimization? Sample efficiency, not scale.

<p className="chapter-lede">Training a neural network, running a wet-lab assay, and calibrating a qubit gate each have the same structural property: one evaluation is expensive, the number of evaluations is small, and the objective has no closed-form gradient. Bayesian optimization addresses this class of problem directly. It maintains a probabilistic model of the objective -- the surrogate -- and uses that model to decide where to evaluate next, extracting the most information from every call to the black box.</p>

## The expensive-evaluation regime

An evaluation is expensive when it takes seconds to hours of wall time, consumes shared physical resources, or cannot be parallelized at low cost. Hyperparameter search for a neural network training run spends GPU-hours per trial. A drug-screening assay takes days per compound. Single-qubit gate calibration on real hardware costs minutes of QPU device time per parameter setting.

Grid search does not solve this problem. It evaluates a Cartesian product of parameter values, most of which lie in regions the objective is already known to be poor. Random search is an improvement -- it avoids the curse of the grid corners -- but it ignores everything it has already learned. Each random trial is drawn from the same prior regardless of what previous trials returned.

The regime that Bayesian optimization targets is roughly 10 to 500 evaluations of a function that takes more than a few seconds per call and has no accessible gradient. Below that budget, random search or a single educated guess is often enough. Above it, the surrogate overhead becomes less justified and cheaper alternatives (evolutionary methods, quasi-random designs) compete more favorably.

## The surrogate-plus-acquisition loop

Bayesian optimization proceeds in four steps, repeated until the budget is exhausted. First, fit a surrogate model to the existing observations. Second, use an acquisition function to identify the next candidate point. Third, evaluate the black box at that point. Fourth, add the result to the observation set and return to step one.<sup className="fn-ref"><a href="#fn-1">[1]</a></sup>

The surrogate is cheap to evaluate because it is a statistical model -- specifically, a Gaussian process in the classical formulation -- not the original objective. All the computation that would otherwise be spent on additional black-box calls goes instead into reasoning about where the next call should be. The acquisition function makes that reasoning concrete: it takes the surrogate's posterior at every candidate input and outputs a scalar that balances the expected improvement against the uncertainty in unexplored regions.<sup className="fn-ref"><a href="#fn-2">[2]</a></sup>

Jones, Schonlau, and Welch formalized this loop in 1998 under the name Efficient Global Optimization (EGO), working in the context of engineering design optimization where simulation runs were the expensive step.<sup className="fn-ref"><a href="#fn-2">[2]</a></sup> The underlying Bayesian framework traces to Mockus in 1975, who cast the problem as sequential decision-making under a prior over functions.<sup className="fn-ref"><a href="#fn-1">[1]</a></sup> Frazier's 2018 tutorial gives the modern pedagogical treatment and is the most useful single reference for a practitioner entering the field.<sup className="fn-ref"><a href="#fn-3">[3]</a></sup>

<figure className="chapter-figure">
  <svg id="bo-ch01-loop" viewBox="0 0 820 200" width="100%" role="img" aria-labelledby="bo-ch01-loop-title bo-ch01-loop-desc" style={{color: 'currentColor'}}>
    <title id="bo-ch01-loop-title">The Bayesian optimization loop: four nodes in a cycle</title>
    <desc id="bo-ch01-loop-desc">A horizontal cycle diagram with four rounded-rectangle nodes connected by arrows. From left to right: Surrogate (GP), Acquisition function, Black-box evaluation, New observation. An arrow from New observation curves back left to Surrogate (GP), completing the loop. The Surrogate node has an accented border and light fill.</desc>

    <defs>
      <marker id="bo-ch01-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto">
        <path d="M0,0 L10,5 L0,10 z" fill="var(--pillar-color, currentColor)" />
      </marker>
    </defs>

    {/* Node 1: Surrogate (GP) -- accented */}
    <rect x="20" y="72" width="160" height="56" rx="8"
      fill="var(--pillar-color, currentColor)" fillOpacity="0.15"
      stroke="var(--pillar-color, currentColor)" strokeWidth="2" />
    <text x="100" y="97" textAnchor="middle" fontSize="13" fontWeight="600" fill="currentColor">Surrogate (GP)</text>
    <text x="100" y="116" textAnchor="middle" fontSize="11" fill="currentColor" opacity="0.75">posterior mean + variance</text>

    {/* Arrow 1 -> 2 */}
    <line x1="180" y1="100" x2="218" y2="100"
      stroke="var(--pillar-color, currentColor)" strokeWidth="1.8"
      markerEnd="url(#bo-ch01-arrow)" />

    {/* Node 2: Acquisition function */}
    <rect x="220" y="72" width="160" height="56" rx="8"
      fill="none" stroke="currentColor" strokeWidth="1.8" />
    <text x="300" y="97" textAnchor="middle" fontSize="13" fontWeight="600" fill="currentColor">Acquisition function</text>
    <text x="300" y="116" textAnchor="middle" fontSize="11" fill="currentColor" opacity="0.75">score each candidate</text>

    {/* Arrow 2 -> 3 */}
    <line x1="380" y1="100" x2="418" y2="100"
      stroke="var(--pillar-color, currentColor)" strokeWidth="1.8"
      markerEnd="url(#bo-ch01-arrow)" />

    {/* Node 3: Black-box evaluation */}
    <rect x="420" y="72" width="180" height="56" rx="8"
      fill="none" stroke="currentColor" strokeWidth="1.8" />
    <text x="510" y="97" textAnchor="middle" fontSize="13" fontWeight="600" fill="currentColor">Black-box evaluation</text>
    <text x="510" y="116" textAnchor="middle" fontSize="11" fill="currentColor" opacity="0.75">GPU run, assay, QPU call</text>

    {/* Arrow 3 -> 4 */}
    <line x1="600" y1="100" x2="638" y2="100"
      stroke="var(--pillar-color, currentColor)" strokeWidth="1.8"
      markerEnd="url(#bo-ch01-arrow)" />

    {/* Node 4: New observation */}
    <rect x="640" y="72" width="160" height="56" rx="8"
      fill="none" stroke="currentColor" strokeWidth="1.8" />
    <text x="720" y="97" textAnchor="middle" fontSize="13" fontWeight="600" fill="currentColor">New observation</text>
    <text x="720" y="116" textAnchor="middle" fontSize="11" fill="currentColor" opacity="0.75">append to dataset</text>

    {/* Return arrow: Node 4 -> Node 1 (arc below) */}
    <path d="M 720 128 Q 720 170 410 170 Q 100 170 100 128"
      fill="none" stroke="var(--pillar-color, currentColor)" strokeWidth="1.8"
      markerEnd="url(#bo-ch01-arrow)" />
  </svg>
  <figcaption>The Bayesian optimization loop. Every step except black-box evaluation is cheap: the GP posterior is a closed-form update, and the acquisition function is optimized by gradient ascent or quasi-random search over the surrogate. Only one black-box call happens per iteration, which is the property that makes the loop sample-efficient.</figcaption>
</figure>

## Why not gradient-based optimization?

The black-box objective is typically not differentiable in any accessible way. The training loss of a model as a function of its hyperparameters is not differentiable because the training run is a discrete, stochastic process. A lab assay returns a scalar measurement with no gradient attached. Even when an automatic differentiation framework could in principle compute a gradient through the simulation, the cost of that computation equals or exceeds the cost of the forward evaluation itself -- which doubles the budget rather than saving it.

Finite-difference gradient estimation is one escape route, but it requires at least one extra evaluation per input dimension. A ten-dimensional hyperparameter space demands eleven evaluations to estimate a single gradient, consuming the entire sample budget in one step. The surrogate avoids this by modeling the objective globally and cheaply, letting the acquisition function be optimized by gradient ascent over the surrogate rather than over the black box.

## Notes

<ol className="footnotes">
  <li id="fn-1">Mockus, J. "On Bayesian Methods for Seeking the Extremum." Optimization Techniques, Springer 1975. Reprinted with extensions in "Bayesian Approach to Global Optimization," Kluwer 1989.</li>
  <li id="fn-2">Jones, D. R., Schonlau, M., and Welch, W. J. "Efficient Global Optimization of Expensive Black-Box Functions." Journal of Global Optimization 13(4), 1998. <a href="https://link.springer.com/article/10.1023/A:1008306431147">https://link.springer.com/article/10.1023/A:1008306431147</a></li>
  <li id="fn-3">Frazier, P. I. "A Tutorial on Bayesian Optimization." arXiv 2018. <a href="https://arxiv.org/abs/1807.02811">https://arxiv.org/abs/1807.02811</a></li>
</ol>
